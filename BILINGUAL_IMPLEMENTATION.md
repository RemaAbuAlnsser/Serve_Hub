# دعم اللغتين العربية والإنجليزية في قاعدة البيانات

## ✅ الأعمدة المضافة

تم إضافة الأعمدة التالية لدعم اللغة الإنجليزية:

### 1. جدول الفئات (categories)
- `name` - الاسم بالعربي
- `name_en` - الاسم بالإنجليزي
- `description` - الوصف بالعربي
- `description_en` - الوصف بالإنجليزي

### 2. جدول الفئات الفرعية (subcategories)
- `name` - الاسم بالعربي
- `name_en` - الاسم بالإنجليزي
- `description` - الوصف بالعربي
- `description_en` - الوصف بالإنجليزي

### 3. جدول المنتجات (products)
- `name` - الاسم بالعربي
- `name_en` - الاسم بالإنجليزي
- `description` - الوصف بالعربي
- `description_en` - الوصف بالإنجليزي

### 4. جدول الشركات (companies)
- `name` - الاسم بالعربي
- `name_en` - الاسم بالإنجليزي

### 5. جدول مدن التوصيل (delivery_cities)
- `city_name` - اسم المدينة بالعربي
- `city_name_en` - اسم المدينة بالإنجليزي

## 📋 الخطوات التالية للتنفيذ

### 1. تحديث نماذج الأدمن (Admin Forms)

يجب تحديث النماذج التالية لإضافة حقول اللغة الإنجليزية:

#### أ. نموذج إضافة/تعديل الفئة
```tsx
// frontend/src/app/admin/categories/page.tsx
- إضافة حقل: Name (English)
- إضافة حقل: Description (English)
```

#### ب. نموذج إضافة/تعديل الفئة الفرعية
```tsx
// frontend/src/app/admin/subcategories/page.tsx
- إضافة حقل: Name (English)
- إضافة حقل: Description (English)
```

#### ج. نموذج إضافة/تعديل المنتج
```tsx
// frontend/src/app/admin/products/page.tsx
- إضافة حقل: Product Name (English)
- إضافة حقل: Description (English)
```

#### د. نموذج إضافة/تعديل الشركة
```tsx
// frontend/src/app/admin/companies/page.tsx
- إضافة حقل: Company Name (English)
```

#### هـ. نموذج إضافة/تعديل مدينة التوصيل
```tsx
// frontend/src/app/admin/delivery/page.tsx
- إضافة حقل: City Name (English)
```

### 2. تحديث Backend APIs

لا حاجة لتعديل الـ Backend APIs - الأعمدة موجودة بالفعل في قاعدة البيانات وستُحفظ تلقائياً عند إرسالها من الـ Frontend.

### 3. تحديث Frontend لعرض اللغة الصحيحة

يجب تحديث المكونات التالية لعرض النص بناءً على اللغة الحالية:

```tsx
// مثال: عرض اسم المنتج حسب اللغة
const { locale } = useLocale();
const productName = locale === 'en' ? product.name_en || product.name : product.name;
const productDescription = locale === 'en' ? product.description_en || product.description : product.description;
```

#### الملفات التي تحتاج تحديث:
- `frontend/src/app/[locale]/page.tsx` - الصفحة الرئيسية
- `frontend/src/app/[locale]/product/[name]/page.tsx` - صفحة المنتج
- `frontend/src/app/[locale]/category/[id]/page.tsx` - صفحة الفئة
- `frontend/src/app/[locale]/deals/page.tsx` - صفحة العروض
- `frontend/src/components/Header.tsx` - القائمة الرئيسية
- `frontend/src/components/CategorySection.tsx` - قسم الفئات
- وغيرها من المكونات التي تعرض بيانات من قاعدة البيانات

## 🎯 ملاحظات مهمة

1. **القيم الافتراضية**: إذا لم يتم إدخال النص الإنجليزي، سيتم عرض النص العربي تلقائياً
2. **التوافق مع الإصدار السابق**: جميع البيانات الموجودة حالياً ستعمل بدون مشاكل
3. **الأدمن**: يمكن للأدمن إضافة النصوص الإنجليزية تدريجياً دون الحاجة لتعبئة كل شيء مرة واحدة

## 📝 مثال على التنفيذ

### في نموذج الأدمن:
```tsx
<input
  type="text"
  placeholder="اسم الفئة (عربي)"
  value={formData.name}
  onChange={(e) => setFormData({...formData, name: e.target.value})}
/>

<input
  type="text"
  placeholder="Category Name (English)"
  value={formData.name_en}
  onChange={(e) => setFormData({...formData, name_en: e.target.value})}
/>
```

### في Frontend:
```tsx
const CategoryCard = ({ category }) => {
  const locale = useLocale();
  const displayName = locale === 'en' && category.name_en 
    ? category.name_en 
    : category.name;
  
  return <h3>{displayName}</h3>;
};
```
