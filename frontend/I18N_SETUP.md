# دعم اللغات المتعددة (العربية والإنجليزية)

تم إضافة دعم كامل للغتين العربية والإنجليزية في الموقع (ما عدا لوحة الإدارة).

## ما تم تنفيذه

### 1. المكتبات المثبتة
- `next-intl` - مكتبة الترجمة لـ Next.js

### 2. الملفات الجديدة

#### ملفات الترجمة
- `messages/ar.json` - الترجمات العربية
- `messages/en.json` - الترجمات الإنجليزية

#### ملفات الإعداد
- `src/i18n.ts` - إعدادات i18n
- `src/middleware.ts` - middleware للتعامل مع اللغات
- `src/components/LanguageSwitcher.tsx` - مكون تبديل اللغة

#### التخطيط الجديد
- `src/app/[locale]/layout.tsx` - layout يدعم اللغات
- تم نقل جميع الصفحات إلى `src/app/[locale]/`

### 3. المكونات المحدثة
- **Header**: تمت إضافة مكون تبديل اللغة وترجمة جميع النصوص
- **Home Page**: تمت ترجمة العناوين والنصوص الرئيسية

## كيفية الاستخدام

### للمستخدم النهائي
1. **اللغة الافتراضية**: العربية
2. **تبديل اللغة**: من خلال أيقونة الكرة الأرضية 🌐 في الهيدر
3. **الروابط**:
   - العربية: `http://localhost:3001/` أو `http://localhost:3001/ar`
   - الإنجليزية: `http://localhost:3001/en`

### للمطورين

#### إضافة ترجمات جديدة
1. افتح `messages/ar.json` و `messages/en.json`
2. أضف المفتاح والقيمة في كلا الملفين:

```json
// messages/ar.json
{
  "common": {
    "newKey": "النص بالعربية"
  }
}

// messages/en.json
{
  "common": {
    "newKey": "Text in English"
  }
}
```

#### استخدام الترجمات في المكونات

```tsx
'use client';
import { useTranslations } from 'next-intl';

export default function MyComponent() {
  const t = useTranslations('common'); // أو 'home', 'product', إلخ
  
  return (
    <div>
      <h1>{t('newKey')}</h1>
    </div>
  );
}
```

#### الحصول على اللغة الحالية

```tsx
import { useLocale } from 'next-intl';

export default function MyComponent() {
  const locale = useLocale(); // 'ar' أو 'en'
  
  return <div>Current language: {locale}</div>;
}
```

#### التنقل مع اللغات

```tsx
import { useRouter } from 'next/navigation';
import { useLocale } from 'next-intl';

export default function MyComponent() {
  const router = useRouter();
  const locale = useLocale();
  
  const goToProduct = (id: number) => {
    router.push(`/${locale}/product/${id}`);
  };
  
  return <button onClick={() => goToProduct(1)}>View Product</button>;
}
```

## الصفحات التي تحتاج ترجمة

الصفحات التالية تم نسخها إلى `[locale]` وتحتاج إلى إضافة الترجمات:

- ✅ `page.tsx` (الرئيسية) - تمت الترجمة
- ⏳ `cart/page.tsx` - تحتاج ترجمة
- ⏳ `checkout/page.tsx` - تحتاج ترجمة
- ⏳ `product/[name]/page.tsx` - تحتاج ترجمة
- ⏳ `category/[id]/page.tsx` - تحتاج ترجمة
- ⏳ `search/page.tsx` - تحتاج ترجمة
- ⏳ `deals/page.tsx` - تحتاج ترجمة
- ⏳ `new/page.tsx` - تحتاج ترجمة

## المكونات التي تحتاج ترجمة

- ✅ `Header.tsx` - تمت الترجمة
- ⏳ `Footer.tsx` - تحتاج ترجمة
- ⏳ `MegaMenu.tsx` - تحتاج ترجمة
- ⏳ `CategorySection.tsx` - تحتاج ترجمة
- ⏳ `SearchDropdown.tsx` - تحتاج ترجمة

## ملاحظات مهمة

1. **لوحة الإدارة**: لم يتم تطبيق الترجمة على `/admin` - تبقى بالعربية فقط
2. **الاتجاه (RTL/LTR)**: يتم تبديله تلقائياً حسب اللغة
3. **الخطوط**: الخطوط العربية (Cairo, Tajawal, Amiri) تعمل مع كلا اللغتين
4. **SEO**: يمكن تحديث metadata لكل لغة في `[locale]/layout.tsx`

## تشغيل المشروع

```bash
cd frontend
npm run dev
```

الموقع سيعمل على: `http://localhost:3001`

## الخطوات التالية (اختيارية)

1. ترجمة باقي الصفحات والمكونات
2. إضافة ترجمات للرسائل (Toast, Modal)
3. ترجمة رسائل الأخطاء
4. إضافة sitemap متعدد اللغات
5. تحسين SEO لكل لغة
